#include<stdio.h>
int main()
{
    int number[15],n;
    for(int j=0; j<16; j++)
    {
        scanf("%d",number[n]);
        for(int i = 0; i<5; i++)
        {
            if(number[n]/2==0)
            {
                printf("par[%d] = %d\n",i,number[n]);
            }
            for(int i = 0; i<5; i++)
            {
                if(number[n]/2 != 0)
                {
                    printf("imapar[%d] = %d",i,number[n]);
                }
            }
        }

    }
}
