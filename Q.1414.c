#include<stdio.h>
int main()
{
    int a,b,c;
    char name[10],goal[3];
    while(scanf("%d %d",&a,&b) != EOF)
    {
        for(int i=0;i<a;i++){
            fgetc(name,sizeof(name),stdin);
        scanf("%d",c);
        int rem;
        if(c%a==1)printf("1\n");
        else if(c%a==2)printf("2\n");
        }

    }
}
